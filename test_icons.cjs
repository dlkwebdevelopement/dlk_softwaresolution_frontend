try {
  const PermMediaIcon = require('@mui/icons-material/PermMedia');
  const VideoLibraryIcon = require('@mui/icons-material/VideoLibrary');
  const PhotoLibraryIcon = require('@mui/icons-material/PhotoLibrary');
  console.log("All icons imported successfully!");
} catch (e) {
  console.error("Icon Import Error:");
  console.error(e.message);
}
